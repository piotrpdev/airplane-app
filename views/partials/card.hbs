<div class="ui card" id ="{{name}}">
  <div class="image">
    <img src="{{image}}">
  </div>
  <div class="content">
    <a class="header">{{fullname}}</a>
    <div class="meta">
      <span class="date">{{yearsEmployed joined}}</span>
    </div>
    <div class="description">
      {{name}} is a {{job}} in {{city}}
    </div>
  </div>
  <div class="extra content">
    <a>
      <i class="user icon"></i>
      {{mates}} Friends
    </a>
  </div>
</div>
